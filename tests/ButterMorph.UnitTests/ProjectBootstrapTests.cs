namespace ButterMorph.UnitTests;

/// <summary>
/// Verifies that the unit test project is discoverable.
/// </summary>
public sealed class ProjectBootstrapTests
{
    /// <summary>
    /// Confirms that the unit test project can execute tests.
    /// </summary>
    [Fact]
    public void UnitTestProjectRuns()
    {
        Assert.True(true);
    }
}
